# Міні-спека: «Фінансові проєкти клієнта» (05-ПРОЕКТИ)

**Статус: ЧЕРНЕТКА на затвердження власником** · 2026-06-12
Джерела: `docs/MODULE_REVIEW_2026-06.md` (05-ПРОЕКТИ, 06-А/Б/В, 12-А/КОМПЕНСАЦІЇ/РЕФЕРАЛ-ПРАЦІВНИКА, 20-Д, 22-нюанси, 02-А/В), `packages/db/prisma/schema.prisma`, `docs/modules/05-billing.md` (Recurring).

Усі місця, де рішення ще НЕ ухвалене, позначені **⚠️ ВІДКРИТЕ** і зведені в розділ 6.

---

## 1. Проблема і вимоги власника

Сьогодні фінансова модель «висить» на двох незвʼязаних механіках: разові `Order`
(fixed/hourly) і recurring-підписки `Service → CompanyService → ServiceCharge`.
Обидві — на рівні **компанії цілком**: одна валюта (`Company.currency`), без
договору, без власного білінг-циклу, без розрізу маржі.

Вимога власника (дослівно за змістом, із проходу 11.06.2026):

> Відносно ОДНОГО клієнта може бути ДЕКІЛЬКА паралельних фінансових моделей —
> «проєктів»/інстансів, кожен зі своїми договором, рахунками, валютою і правилами.
>
> - Приклад: (1) «Оренда» — щомісячне фіксоване АВАНСОВЕ нарахування, свій
>   договір/рахунки; (2) «Програмування» — чисто погодинні роботи, своя ставка;
>   (3) інші послуги — інша ставка/абонплата, інший договір.
> - **Абонплати — ЗАВЖДИ авансові. Погодинні — авансові АБО по факту** (налаштування моделі).
> - Різні проєкти одного клієнта — можуть бути в РІЗНИХ валютах.
> - Договір: для якихось типів обовʼязковий, для якихось опційний — продумати обовʼязковість полів.
> - **Маржа per-проєкт/per-виконавець**: клієнт платить **$30/год** по проєкту →
>   програміст отримує **$10/год** → агенція лишає **$20**. Ставка-собівартість: на
>   рівні проєкту/людини/відділу — структуру продумати (власник відкритий: або
>   привʼязка до проєкту, або чисто на стороні працівників).

Доповнення власника (11.06): **задачі, ВКЛЮЧЕНІ в абонплату.** Клієнт платить
фіксовану абонплату ($100/міс) → у рамках неї команда виконує роботи; ці задачі
створюються НА ПРОЄКТІ, падають у канбан виконавцям як звичайні, по них логується
час, але вони **НЕ білляться окремо** (режим `included_in_subscription`,
zero-billed). Їхні години = собівартість проєкту → маржа абон-проєкту =
абонплата − Σ(год × ставка-собівартість). Власник бачить, чи абонплата вигідна.

Звідси функціональні вимоги:

| #   | Вимога                                                                                                                                | Звідки                  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| В1  | N проєктів на 1 клієнта, кожен зі своєю фін-моделлю                                                                                   | 05-ПРОЕКТИ              |
| В2  | 3 білінг-моделі: абонплата-аванс / погодинка-аванс / погодинка-факт                                                                   | 05-ПРОЕКТИ              |
| В3  | Валюта per-проєкт (не per-компанія)                                                                                                   | 05-ПРОЕКТИ, 05-Е        |
| В4  | Договір per-проєкт; обовʼязковість залежить від типу і конфігурується                                                                 | 05-ПРОЕКТИ, 06-А        |
| В5  | Білінг-цикл per-проєкт: N-числа місяця / щотижня (день X) / вручну; автоакти/автодокументи по закриттю циклу                          | 06-В                    |
| В6  | Маржа per-проєкт/per-виконавець: клієнтська ставка − собівартість; собівартість — override на проєкті з fallback на ставку працівника | 05-ПРОЕКТИ, 12-А, 22-Д  |
| В7  | «Нульова собівартість» виконавця (owner/партнери): cost=0, весь дохід — агенції                                                       | 22-нюанси               |
| В8  | Реферал-працівника: % від ЧИСТОГО доходу (маржі) по приведеному клієнту                                                               | 12-РЕФЕРАЛ-ПРАЦІВНИКА   |
| В9  | Юр-особа агенції per-проєкт для договорів і рахунків                                                                                  | 20-Д                    |
| В10 | Payment terms per-проєкт (дефолт ↑ компанія ↑ агенція)                                                                                | 05-Г                    |
| В11 | Опційні гейти: погодження оцінки (02-А), аванс-гейт N% (02-В) — дефолти на рівні проєкту                                              | 02-А, 02-В              |
| В12 | Zero-billed задачі в рамках абонплати, їхні години — у собівартість                                                                   | 05-ПРОЕКТИ (доповнення) |

---

## 2. Модель даних (Prisma-ескіз)

### 2.1. Нові сутності

```prisma
enum ProjectBillingModel {
  fixed_monthly_advance // абонплата; нарахування ЗАВЖДИ авансом на початку циклу
  hourly_prepaid        // погодинка з авансом (гейт 02-В)
  hourly_postpaid       // погодинка по факту: нарахування при закритті циклу
}

enum ProjectBillingCycle {
  monthly_day_n // кожного N-числа місяця (cycleDay)
  weekly_day_x  // щотижня у день X (cycleWeekday; кейс власника — «щопонеділка»)
  manual        // «здав проєкт — закрив руками — сформував документи»
}

/// Фінансовий проєкт клієнта (05-ПРОЕКТИ). Контейнер фін-моделі між Company
/// та Orders/Charges: договір, валюта, цикл, ставки, маржа — все per-проєкт.
model Project {
  id        String  @id @default(uuid())
  agencyId  String
  companyId String
  company   Company @relation(fields: [companyId], references: [id])
  name      String                    // «Оренда», «Програмування», …
  type      String?                   // категорія для звітів; ⚠️ ВІДКРИТЕ (П8): текст чи довідник

  billingModel     ProjectBillingModel
  currency         String  @default("USD")          // per-проєкт (В3), НЕ Company.currency
  abonAmount       Decimal? @db.Decimal(12, 2)      // fixed_monthly_advance: сума абонплати за цикл
  clientHourlyRate Decimal? @db.Decimal(10, 2)      // hourly_*: клієнтська ставка ($30 у прикладі)

  billingCycle ProjectBillingCycle @default(monthly_day_n)
  cycleDay     Int?      // 1..28 для monthly_day_n
  cycleWeekday Int?      // 1..7 (ISO; 1=понеділок) для weekly_day_x
  nextCycleAt  DateTime? @db.Timestamptz(3) // якір наступного закриття (патерн CompanyService.nextChargeAt); null для manual

  paymentTermsDays Int?     // 05-Г; null → дефолт компанії → дефолт агенції
  legalEntityId    String?  // 20-Д: юр-особа агенції для договорів/рахунків; null → дефолтна юр-особа
  // legalEntity   LegalEntity? — модель LegalEntity йде окремою міні-спекою 20-Д (передумова)

  contractRequired   Boolean @default(false)  // дефолт залежить від billingModel — див. §5
  contractDocumentId String? // Document(type=contract) / завантажений підписаний (06-Б upload)

  requiresApproval Boolean?              // 02-А; null → дефолт компанії
  advanceGatePct   Decimal? @db.Decimal(5, 2) // 02-В; має сенс для hourly_prepaid (і fixed — аванс 100%)

  includedHoursCap Decimal? @db.Decimal(8, 2) // ⚠️ ВІДКРИТЕ (П1): ліміт включених годин абонплати

  active    Boolean  @default(true)
  createdAt DateTime @default(now()) @db.Timestamptz(3)
  updatedAt DateTime @updatedAt @db.Timestamptz(3)

  executorRates ProjectExecutorRate[]
  orders        Order[]
  charges       ServiceCharge[]

  @@unique([companyId, name])
  @@index([agencyId])
  @@index([agencyId, active])
  @@index([companyId])
  @@map("projects")
}

/// Override ставки-СОБІВАРТОСТІ виконавця на проєкті (12-А). Якщо рядка немає —
/// fallback на персональну ставку працівника (ExecutorRate.hourlyRate, 12-КОМПЕНСАЦІЇ).
model ProjectExecutorRate {
  id             String   @id @default(uuid())
  agencyId       String
  projectId      String
  project        Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  executorId     String
  executor       Profile  @relation(fields: [executorId], references: [id], onDelete: Cascade)
  costHourlyRate Decimal? @db.Decimal(10, 2) // собівартість год на ЦЬОМУ проєкті ($10 у прикладі)
  currency       String   @default("USD")
  zeroCost       Boolean  @default(false)    // 22: «без собівартості / весь дохід агенції» — перекриває все, cost=0
  createdAt      DateTime @default(now()) @db.Timestamptz(3)
  updatedAt      DateTime @updatedAt @db.Timestamptz(3)

  @@unique([projectId, executorId])
  @@index([agencyId])
  @@map("project_executor_rates")
}
```

### 2.2. Зміни наявних моделей

```prisma
model Order {
  // + привʼязка замовлення до фін-проєкту (опційна — legacy-замовлення без проєкту валідні)
  projectId String?
  project   Project? @relation(fields: [projectId], references: [id])
  // + режим zero-billed для задач, включених в абонплату (В12)
  zeroBilled Boolean @default(false) // true: НЕ білиться, але TimeLog → собівартість проєкту
  @@index([projectId])
}

model ServiceCharge {
  // + нарахування може походити з проєкту, а не лише з CompanyService
  projectId        String?  // нарахування циклу проєкту
  project          Project? @relation(...)
  companyServiceId String?  // ⚠️ стає NULLABLE (зараз обовʼязкове) — див. §2.4 міграцію
  periodStart      DateTime? @db.Date // межі циклу (для weekly `month` недостатньо)
  periodEnd        DateTime? @db.Date
  @@unique([projectId, periodStart]) // ідемпотентність генерації per-цикл (аналог @@unique([companyServiceId, month]))
}

model ExecutorRate {
  // 12-КОМПЕНСАЦІЇ: давно запланована колонка — БАЗОВА ставка-собівартість працівника
  hourlyRate      Decimal? @db.Decimal(10, 2)
  // 22: глобальний дефолт «нульової собівартості» (owner за замовчуванням; owner може дати будь-кому)
  zeroCostDefault Boolean  @default(false)
}

model TimeLog {
  // ⚠️ ВІДКРИТЕ (П5): снапшот резолвнутих ставок у момент логування —
  // пропозиція: ТАК (стабільна історія маржі при зміні ставок)
  clientRateSnapshot Decimal? @db.Decimal(10, 2)
  costRateSnapshot   Decimal? @db.Decimal(10, 2)
  costCurrency       String?
}

model Company {
  // 12-РЕФЕРАЛ-ПРАЦІВНИКА: хто з працівників привів клієнта
  referredByEmployeeId String?
}

model ExecutorPayout {
  // 12-РЕФЕРАЛ-ПРАЦІВНИКА: окремий компонент виплати
  referralBonusAmount Decimal @default(0) @db.Decimal(10, 2)
}
```

### 2.3. Резолюція ставок (канон)

Для кожного `TimeLog` на замовленні з `projectId`:

- **Клієнтська ставка** (дохід): `Project.clientHourlyRate`. Для zero-billed
  задач абонплати — `0` (дохід циклу = `abonAmount`). Для legacy-замовлень без
  проєкту — `Order.hourlyRate` (як зараз).
- **Собівартість** (каскад, перший збіг перемагає):
  1. `ProjectExecutorRate.zeroCost = true` → **0** (весь дохід — агенції);
  2. `ProjectExecutorRate.costHourlyRate` (override на проєкті);
  3. `ExecutorRate.zeroCostDefault = true` → **0** (owner/партнери, 22);
  4. `ExecutorRate.hourlyRate` (базова ставка працівника, 12-КОМПЕНСАЦІЇ);
  5. немає нічого → **0** + ворнінг у UI («ставку не задано») — маржа завищена свідомо, не блокуємо логування.

### 2.4. Співіснування і еволюція CompanyService (міграційна нотатка)

Канон recurring сьогодні — `Service → CompanyService → ServiceCharge`
(рішення R3, `docs/modules/05-billing.md`); генерація — `recurringCharges.ts`
по якорю `nextChargeAt`, ідемпотентність через `@@unique([companyServiceId, month])`.

**Цільовий стан:** recurring-підписка клієнта — це окремий випадок проєкту
`fixed_monthly_advance`. Тобто **CompanyService поглинається Project-ом**.

Пропонований шлях (двофазний, без big-bang):

- **Фаза 1 (співіснування):** `ServiceCharge.companyServiceId` стає nullable,
  додається `projectId`. Нові проєкти генерують charges зі своїм `projectId`;
  старі CompanyService продовжують працювати як є. Statement/debtors/P&L
  читають `service_charges` як і раніше — джерело одне, нічого не ламається.
- **Фаза 2 (міграція):** скрипт перетворює кожен активний `CompanyService` на
  `Project { name: service.name, billingModel: fixed_monthly_advance,
abonAmount: customPrice, currency: company.currency, billingCycle:
monthly_day_n, cycleDay: 1, nextCycleAt: nextChargeAt }`; історичні charges
  отримують backfill `projectId`; `CompanyService.active=false` (рядки лишаються
  для історії, нових charges не породжують). Каталог `Service` лишається як
  довідник «що ми продаємо» і джерело дефолтних цін при створенні проєкту.
- ⚠️ ВІДКРИТЕ (П6): мігрувати все одразу у Фазі 2 чи тримати співіснування довше.

---

## 3. Білінг-цикли (звʼязка з 06-В)

Двигун — еволюція наявного `generateRecurringCharges` (той самий патерн:
якір `nextCycleAt`, `MAX_CATCHUP_PERIODS`-backstop, ідемпотентність через
`@@unique([projectId, periodStart])`, кросс-тенантний cron + ручний replay).

**Закриття циклу** (cron щодня, `WHERE nextCycleAt <= now() AND active`):

| Крок                    | fixed_monthly_advance                                                                                   | hourly_postpaid                                                                           | hourly_prepaid                                                                                                      |
| ----------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| 1. Нарахування          | `ServiceCharge{projectId, amount: abonAmount}` **авансом на НАСТУПНИЙ період** (абонплата завжди аванс) | `ServiceCharge{amount: Σ(год циклу × clientHourlyRate)}` **за минулий період** (по факту) | аванс виставляється при старті робіт (гейт 02-В); закриття циклу — звірка факту проти авансу, донарахування різниці |
| 2. dueDate              | `paymentTermsDays` від дати виставлення (05-Г) → dunning 05-Б                                           | так само                                                                                  | так само                                                                                                            |
| 3. Автодокументи (06-В) | рахунок на період + **акт за минулий цикл** (виконані роботи, год по zero-billed задачах)               | рахунок + акт з деталізацією годин                                                        | акт звірки авансу                                                                                                   |
| 4. Доставка             | email компанії «для документів» (06-Б) + Telegram (06-Г); трекінг доставки                              | так само                                                                                  | так само                                                                                                            |
| 5. Зсув якоря           | `nextCycleAt += 1 міс` (на cycleDay)                                                                    | `+1 міс` або `+7 днів` (weekly)                                                           | так само                                                                                                            |

Режими запуску закриття:

- **`monthly_day_n`** — авто кожного N-числа (cycleDay 1..28).
- **`weekly_day_x`** — авто щотижня у день X; кейс власника: «щопонеділка
  авто-закриття зданих задач + відправка документів».
- **`manual`** — `nextCycleAt = null`; кнопка «Закрити цикл» у Workspace
  («здав проєкт — закрив руками — сформував документи») викликає той самий
  код закриття з `periodStart/End`, що їх задає owner.

Автосписання з передоплати (05-Д): нове нарахування циклу гаситься з
`moneyBalance` через наявний `PaymentAllocation`/`refreshMoneyBalance` — без змін
механіки, лише `chargeId` тепер може вказувати на проєктний charge.

Журналювання: кожен запуск двигуна — рядок у `CronRun` (21-А) — грошовий
автоцикл, фейл має алертити.

---

## 4. Маржа

### 4.1. Формули (двигун — 22-Д cost allocation, піднятий у ядро)

Для проєкту `P` за період `[t1, t2]` (цикл або довільний діапазон звіту):

```
виручка(P)      = Σ ServiceCharge.totalAmount (projectId = P, period ∈ [t1,t2])     // accrual
собівартість(P) = Σ по TimeLog замовлень проєкту: hours × costRateSnapshot          // §2.3
маржа(P)        = виручка(P) − собівартість(P)
маржа%          = маржа(P) / виручка(P)
```

- Для абон-проєктів собівартість включає години **zero-billed** задач — саме так
  власник бачить, «чи абонплата вигідна» (приклад: $100 абонплата − 6 год × $12 = $28 маржі).
- Для погодинних — перевірка прикладу власника: 1 год → виручка $30, собівартість
  $10, маржа $20. Per-виконавець розріз: групування тих самих TimeLog по `executorId`.
- **Валюти:** проєкти в різних валютах; зведені звіти — конвертація в USD по
  `exchange_rates` (патерн `Payment.amountUsd`: снапшот курсу, не latest).
  ⚠️ ВІДКРИТЕ (П7): курс для собівартості — на дату TimeLog чи на дату закриття циклу.
- ⚠️ ВІДКРИТЕ (П2): маржа accrual (по нарахуваннях, пропозиція-дефолт) чи
  cash-basis (по оплатах). Пропозиція: accrual + окремий індикатор «оплачено N%».

### 4.2. Реферал-працівника (12-РЕФЕРАЛ-ПРАЦІВНИКА)

Працівник привів клієнта (`Company.referredByEmployeeId`) → отримує
налаштовуваний **% від ЧИСТОГО доходу** агенції з цього клієнта. Приклад
власника: клієнт платить $20/год, виконавець коштує $10/год → чистих $10/год →
% рефереру. **Неважливо, хто виконує роботи — важливо, хто привів.**

```
чистий_дохід(клієнт, період) = Σ маржа(P) по всіх проєктах клієнта   // та сама формула §4.1
реферал_бонус = чистий_дохід × refPct (per-agency налаштування)
```

Нараховується у `ExecutorPayout.referralBonusAmount` окремим рядком виплати.
⚠️ ВІДКРИТЕ (П4): рівень агрегації — клієнт цілком чи per-проєкт/замовлення
(власник казав «або замовлення — рівень агрегації в налаштуваннях»).

### 4.3. Видимість собівартості (22-нюанси)

- Собівартість/маржа видимі **адміну агенції** (на SaaS — тенант-адмінам):
  загалом і в розрізі проєкту.
- Виконавці НЕ бачать клієнтських ставок і маржі — лише свої ставки/виплати.
- Прапорець «нульова собівартість»: у owner-а за замовчуванням
  (`ExecutorRate.zeroCostDefault`), owner може призначити будь-кому
  (партнери-співвласники); per-проєктне перекриття — `ProjectExecutorRate.zeroCost`.

---

## 5. Обовʼязковість полів по типах

«Договір: для якихось типів обовʼязковий, для якихось опційний» — реалізуємо як
**конфігуровані дефолти per-тип** + явний прапорець на проєкті.

| Поле                        | fixed_monthly_advance                              | hourly_prepaid                                | hourly_postpaid                  |
| --------------------------- | -------------------------------------------------- | --------------------------------------------- | -------------------------------- |
| `abonAmount`                | **обовʼязкове**                                    | —                                             | —                                |
| `clientHourlyRate`          | опційне (для робіт понад ліміт, якщо П1=так)       | **обовʼязкове**                               | **обовʼязкове**                  |
| `currency`                  | **обовʼязкове** (дефолт з компанії)                | так само                                      | так само                         |
| `billingCycle`              | дефолт `monthly_day_n` (cycleDay=1)                | будь-який                                     | будь-який (типово weekly/manual) |
| `contractRequired` (дефолт) | **true** — абонплата без договору не активується   | false                                         | false                            |
| `contractDocumentId`        | вимагається при активації, якщо `contractRequired` | так само                                      | так само                         |
| `advanceGatePct` (02-В)     | n/a (аванс = 100% абонплати за визначенням)        | релевантне (дефолт компанії)                  | —                                |
| `requiresApproval` (02-А)   | —                                                  | релевантне (погоджується ставка + оцінка год) | релевантне                       |
| `paymentTermsDays`          | опційне → каскад компанія → агенція                | так само                                      | так само                         |
| `legalEntityId`             | опційне → дефолтна юр-особа агенції                | так само                                      | так само                         |

Правила активації (`active=true`):

- `contractRequired && !contractDocumentId` → ⚠️ ВІДКРИТЕ (П3): блок активації
  чи активація з ворнінгом і блоком лише генерації документів.
- Дефолти `contractRequired` per-тип — редаговані в налаштуваннях агенції
  (власник: «продумати обовʼязковість полів», тож зашиваємо політику, не хардкод).

---

## 6. Відкриті питання власнику (на затвердження)

| #   | Питання                                                                                                                                                                                                                         | Пропозиція-дефолт                            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| П1  | **Ліміт включених годин в абонплаті?** Чи потрібен `includedHoursCap` з автоматичним переходом у погодинку (`clientHourlyRate`) понад ліміт — чи абонплата покриває все без ліміту? (зафіксовано як невирішене у проході 11.06) | Поле закладаємо, логіку понадліміту — фаза 2 |
| П2  | **Маржа: accrual чи cash?** Рахувати від нарахувань (виставили — порахували) чи лише від підтверджених оплат?                                                                                                                   | Accrual + бейдж «оплачено N%»                |
| П3  | **Договір-гейт:** без підписаного договору — блокувати активацію проєкту повністю чи дозволяти роботу, але блокувати рахунки/акти?                                                                                              | Ворнінг + блок лише документів               |
| П4  | **Реферал-працівника — рівень агрегації:** % від чистого доходу по клієнту цілком чи per-проєкт/замовлення (з можливістю різних %)?                                                                                             | По клієнту цілком, один %                    |
| П5  | **Снапшот ставок на TimeLog:** фіксувати клієнтську/собівартісну ставку в момент логування (стабільна історія) чи завжди перераховувати від поточних ставок?                                                                    | Снапшот у момент логування                   |
| П6  | **Міграція CompanyService:** перетворити всі наявні recurring-підписки на проєкти одним скриптом (фаза 2 зразу після релізу) чи тримати дві механіки до ручного перенесення?                                                    | Один скрипт, після 2-3 тижнів співіснування  |
| П7  | **Курс валют для собівартості** (ставка виконавця в UAH, проєкт у USD): конвертувати на дату TimeLog чи на дату закриття циклу?                                                                                                 | На дату TimeLog (узгоджено зі снапшотом П5)  |
| П8  | **`Project.type`:** вільний текст («оренда», «програмування») чи довідник категорій per-agency для звітів?                                                                                                                      | Вільний текст зараз, довідник на SaaS-фазі   |

---

## 7. Вплив на наявний код

### Схема (`packages/db/prisma/schema.prisma`)

| Обʼєкт                           | Зміна                                                                                                |
| -------------------------------- | ---------------------------------------------------------------------------------------------------- |
| `Project`, `ProjectExecutorRate` | нові таблиці (+ RLS-політики agencyId, як у всіх tenant-таблиць)                                     |
| `LegalEntity` (20-Д)             | нова таблиця — **передумова**, окрема міні-спека; тут лише FK                                        |
| `ServiceCharge`                  | `companyServiceId` → nullable; + `projectId`, `periodStart/End`; + unique `(projectId, periodStart)` |
| `Order`                          | + `projectId?`, `zeroBilled`                                                                         |
| `TimeLog`                        | + снапшоти ставок (П5)                                                                               |
| `ExecutorRate`                   | + `hourlyRate` (12-КОМПЕНСАЦІЇ), `zeroCostDefault` (22)                                              |
| `Company`                        | + `referredByEmployeeId` (12-РЕФЕРАЛ)                                                                |
| `ExecutorPayout`                 | + `referralBonusAmount`                                                                              |

### Бекенд (`apps/api/src`)

| Файл/сервіс                                                 | Зміна                                                                                                                                                 |
| ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `services/recurringCharges.ts` + `cron/recurringCharges.ts` | еволюція в цикл-двигун проєктів (§3): три моделі нарахування, weekly/manual режими, periodStart/End; legacy-гілка CompanyService на час співіснування |
| `services/allocation.ts`                                    | без зміни механіки; allocations покривають проєктні charges (chargeId той самий)                                                                      |
| `services/payout.ts`                                        | `hourlyEarned` нарешті вмикається (`ExecutorRate.hourlyRate` + project-override); + компонент `referralBonusAmount`                                   |
| `services/statement.ts`                                     | + розріз/фільтр по `projectId` у таймлайні                                                                                                            |
| `services/pnl.ts`                                           | собівартість/маржа по проєктах (22-Д у ядро), zero-cost правила                                                                                       |
| `services/referral.ts`                                      | поруч — новий employee-referral accrual (механіка ідемпотентності як `ReferralBonus @@unique(sourceType, sourceId)`)                                  |
| `routes/services/*`, `routes/billing/charges.ts`            | + CRUD `/workspace/companies/:id/projects`, ставки проєкту, ручне закриття циклу                                                                      |
| Документи (модуль 06, S6)                                   | генерація рахунків/актів per-цикл від юр-особи проєкту; `Document` вже має `companyId/orderId` — + `projectId?`                                       |
| `cron/index.ts` + `CronRun` (21-А)                          | реєстрація циклу + журнал/алерти                                                                                                                      |

### Фронтенд

- **Workspace:** блок екранів «Проєкти клієнта» (список/створення/налаштування,
  ставки виконавців, закриття циклу, маржа-розріз) — у ТЗ дизайнеру.
- **Portal:** розрізи білінгу по проєктах (рахунки/акти/борг per-проєкт).

### Критична залежність послідовності (з фінального підсумку проходу)

Проєкти (05) + юр-особи (20-Д) + реквізити клієнта (06-Б) + payment terms (05-Г)
мають лягти **ДО/РАЗОМ зі спринтом документів S6** — інакше S6 будуватиме рахунки
на старій моделі і доведеться переробляти.

---

_Чернетка підготовлена для проходу власника. Після затвердження П1-П8 — фіналізація
схеми, ТЗ дизайнеру (блок екранів «Проєкти клієнта») і розбивка на спринти._
