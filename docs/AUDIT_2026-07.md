# 🔎 АУДИТ 2026-07-01 — велика Фаза-B сесія (28 зрізів) + ремедіація

> Канон-запис аудиту коду, доданого з останнього повного аудиту
> ([`AUDIT_FULL_2026-06.md`](AUDIT_FULL_2026-06.md), 11.06) і пізніших S5.6/S6 аудитів.
> Метод: 3-вимірний багатоагентний аудит (security · typescript · code-quality), кожен над
> наскрізним діфом сесії `8656538~1..a24409d` (73 файли, ~6.9k рядків). Усі знахідки
> верифіковано й **виправлено** (commit `f64c67b`). Гейт зелений на кожному кроці.

## Що збудовано за сесію (з попереднього аудиту)

Випереджальна Фаза-B у межах MVP-обсягу (S0–S8). Усе на `dev`, кожен зріз: гейт →
коміт → пуш → оновлення доків. **api-тести 453 → 572 unit + 151 gated; 3 нові міграції
(усі drift-free, перевірені на throwaway PG16 + `migrate deploy`).**

| Блок                                               | Що                                                                                                                                                                                                    | Комміти                                 |
| -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| **S7 SEO**                                         | canonical · динамічний OG-образ · sitemap (cases+blog)                                                                                                                                                | `8656538`                               |
| **S6 харден**                                      | backend route-тести: Notifications · Documents · Bot/Telegram · profile-notifications                                                                                                                 | `9efaffc`·`718f9be`·`3f269f5`·`4b78498` |
| **28-Б Картка клієнта 360°** (дизайн-пріоритет №1) | Лояльність(+override) · Маржа(YTD+проєкти) · Реквізити(read+edit on-behalf) · Проєкти · **таб-shell** · «Потребує уваги» · **Люди**(roster+role/remove) · **Документи**(PDF) — 6 нових бек-ендпоінтів | `c4f14d4`…`5b41d66`·`e2356bf`·`3b4b466` |
| **Orders-v2**                                      | таб «Специфікація» + звʼязок замовлення↔проєкт; **глобальна «Дошка задач»**                                                                                                                           | `b228564`·`8265c19`                     |
| **T2 Таймер**                                      | модель+міграція · сервіс (advisory-lock one-active) · cron C15 · UI (floating-bar + order-control) · real-PG харден                                                                                   | `62881dd`·`3e185bd`·`a1a9f6a`           |
| **Reports**                                        | план-факт годин (оцінка vs факт) + **capacity-завантаження** (норма×тижні)                                                                                                                            | `bdbb6a1`·`2225dbe`                     |
| **Портал**                                         | грейсфул «немає компанії» (CompanyGate) · портал «Проєкти» (read)                                                                                                                                     | `c442b29`·`a86b911`                     |
| **Leads/CRM (26)**                                 | модель+міграція+RLS · board/create/update/convert/delete · DnD-канбан                                                                                                                                 | `a24409d`                               |

**Знахідки ручного тесту #1–8 — усі закриті.** Картка 360° (28-Б) — 6/7 табів на реальному
беку (7-й «Секрети»/vault = окремий модуль). T2 + глобальна дошка закрили лишок Orders-v2.

## Аудит: знахідки та виправлення (всі — `f64c67b`)

**Security (0 critical/high).** Tenant-scoping (RLS + agencyId), ролі, IDOR — консистентні на
кожному новому ендпоінті. Portal `/portal/projects` DTO не тече внутрішні поля (перевірено
тестом). Leads convert валідує і лід, і компанію в тенанті. RLS на `leads` коректний.

| Sev                     | Знахідка                                                                                                                  | Виправлення                                                                                      |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| **HIGH** (TS)           | Timer `dateOnly` брав день у UTC → Kyiv-таймер 00:00–03:00 датувався попереднім днем (скіс період-локу ЗП + дати у звіті) | `dateOnly` → Europe/Kyiv бізнес-день (UTC-північ локального дня, як ручний лог)                  |
| **HIGH** (code)         | Drag ліда в «Виграно» обходив convert → `status=won` без order/company                                                    | бек: PATCH відхиляє `status=won` (лише через convert); фронт: won-drop → convert-модалка         |
| **MED** (security/code) | Last-owner guard + Lead convert: `count/findFirst`→`write` під READ COMMITTED без локу → орфан-компанія / дубль-Order     | `pg_advisory_xact_lock` per-company (members) і per-lead (convert) — той самий патерн, що таймер |
| **LOW** (TS)            | Cron auto-stop не брав advisory-lock (гонка з user-stop)                                                                  | lock per-executor у sweep-циклі                                                                  |
| nit                     | `ClientProject` без `createdAt`; широкий тип `useUpdateLead`                                                              | дотягнуто (контракт точний; тип без `won`)                                                       |

**Регресій НЕ знайдено** у змінених існуючих файлах: `timeLogs.ts` (фільтр running-таймерів не
ламає ручні логи/total), `getOrder.ts` (project additive), `internalTasks.ts` (route не
шадовить), `reports.ts` (pnl недоторканий), `ClientDetailPage` (tab-рефактор: hooks-order ок,
Overview збережено, manager-гейти=бек-403), `OrderDetailPage`/`TimeTab`, `CompanyGate` (жоден
маршрут не загублено).

## Відповідність дизайну (нові екрани = свідомі субсети)

Кожен новий екран — backend-backed субсет повного мокапа (RISK-нотатки у
[`DESIGN_COVERAGE.md`](DESIGN_COVERAGE.md)):

- **Leads** — ядро борду; **ще нема**: lead-detail `/leads/:id`, редактор пайплайнів,
  іконки джерел, UTM-захоплення з форми, суми угод по колонках, % конверсії, lost-аналітика.
- **Картка 360°** — 6/7 табів; нема «Секрети» (vault-модуль), агенційне edit-member обмежене
  (роль/видалити; нема reset-password/invite); People/Activity-rich-поля з мокапа.
- **Reports** — план-факт за оцінкою+capacity; нема 4-рівневого проєкт/місяць-розрізу дизайну.
- **Дошка задач** — cross-order DnD; нема per-team-boards/timeline-в'ю.
- **Project360** — білінг+юр-особа+маржа+close-cycle+нарахування; нема hero-KPI/hours-bar/задач.

## Стан гейту

type-check 23 · lint 14 · build 14 · test (**api 572 unit + 151 gated real-PG** · types 104) ·
3 міграції drift-free. CI `db-gate.yml` на пуші: migration-smoke + drift + RLS-isolation.
