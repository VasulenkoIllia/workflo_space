# 🔎 АУДИТ 2026-07-01 — велика Фаза-B сесія (28 зрізів) + ремедіація

> Канон-запис аудиту коду, доданого з останнього повного аудиту
> ([`AUDIT_FULL_2026-06.md`](AUDIT_FULL_2026-06.md), 11.06) і пізніших S5.6/S6 аудитів.
> Метод: 3-вимірний багатоагентний аудит (security · typescript · code-quality), кожен над
> наскрізним діфом сесії `8656538~1..a24409d` (73 файли, ~6.9k рядків). Усі знахідки
> верифіковано й **виправлено** (commit `f64c67b`). Гейт зелений на кожному кроці.

## Що збудовано за сесію (з попереднього аудиту)

Випереджальна Фаза-B у межах MVP-обсягу (S0–S8). Усе на `dev`, кожен зріз: гейт →
коміт → пуш → оновлення доків. **api-тести 453 → 572 unit + 151 gated; 3 нові міграції
(усі drift-free, перевірені на throwaway PG16 + `migrate deploy`).**

| Блок                                               | Що                                                                                                                                                                                                    | Комміти                                 |
| -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| **S7 SEO**                                         | canonical · динамічний OG-образ · sitemap (cases+blog)                                                                                                                                                | `8656538`                               |
| **S6 харден**                                      | backend route-тести: Notifications · Documents · Bot/Telegram · profile-notifications                                                                                                                 | `9efaffc`·`718f9be`·`3f269f5`·`4b78498` |
| **28-Б Картка клієнта 360°** (дизайн-пріоритет №1) | Лояльність(+override) · Маржа(YTD+проєкти) · Реквізити(read+edit on-behalf) · Проєкти · **таб-shell** · «Потребує уваги» · **Люди**(roster+role/remove) · **Документи**(PDF) — 6 нових бек-ендпоінтів | `c4f14d4`…`5b41d66`·`e2356bf`·`3b4b466` |
| **Orders-v2**                                      | таб «Специфікація» + звʼязок замовлення↔проєкт; **глобальна «Дошка задач»**                                                                                                                           | `b228564`·`8265c19`                     |
| **T2 Таймер**                                      | модель+міграція · сервіс (advisory-lock one-active) · cron C15 · UI (floating-bar + order-control) · real-PG харден                                                                                   | `62881dd`·`3e185bd`·`a1a9f6a`           |
| **Reports**                                        | план-факт годин (оцінка vs факт) + **capacity-завантаження** (норма×тижні)                                                                                                                            | `bdbb6a1`·`2225dbe`                     |
| **Портал**                                         | грейсфул «немає компанії» (CompanyGate) · портал «Проєкти» (read)                                                                                                                                     | `c442b29`·`a86b911`                     |
| **Leads/CRM (26)**                                 | модель+міграція+RLS · board/create/update/convert/delete · DnD-канбан                                                                                                                                 | `a24409d`                               |

**Знахідки ручного тесту #1–8 — усі закриті.** Картка 360° (28-Б) — 6/7 табів на реальному
беку (7-й «Секрети»/vault = окремий модуль). T2 + глобальна дошка закрили лишок Orders-v2.

## Аудит: знахідки та виправлення (всі — `f64c67b`)

**Security (0 critical/high).** Tenant-scoping (RLS + agencyId), ролі, IDOR — консистентні на
кожному новому ендпоінті. Portal `/portal/projects` DTO не тече внутрішні поля (перевірено
тестом). Leads convert валідує і лід, і компанію в тенанті. RLS на `leads` коректний.

| Sev                     | Знахідка                                                                                                                  | Виправлення                                                                                      |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| **HIGH** (TS)           | Timer `dateOnly` брав день у UTC → Kyiv-таймер 00:00–03:00 датувався попереднім днем (скіс період-локу ЗП + дати у звіті) | `dateOnly` → Europe/Kyiv бізнес-день (UTC-північ локального дня, як ручний лог)                  |
| **HIGH** (code)         | Drag ліда в «Виграно» обходив convert → `status=won` без order/company                                                    | бек: PATCH відхиляє `status=won` (лише через convert); фронт: won-drop → convert-модалка         |
| **MED** (security/code) | Last-owner guard + Lead convert: `count/findFirst`→`write` під READ COMMITTED без локу → орфан-компанія / дубль-Order     | `pg_advisory_xact_lock` per-company (members) і per-lead (convert) — той самий патерн, що таймер |
| **LOW** (TS)            | Cron auto-stop не брав advisory-lock (гонка з user-stop)                                                                  | lock per-executor у sweep-циклі                                                                  |
| nit                     | `ClientProject` без `createdAt`; широкий тип `useUpdateLead`                                                              | дотягнуто (контракт точний; тип без `won`)                                                       |

**Регресій НЕ знайдено** у змінених існуючих файлах: `timeLogs.ts` (фільтр running-таймерів не
ламає ручні логи/total), `getOrder.ts` (project additive), `internalTasks.ts` (route не
шадовить), `reports.ts` (pnl недоторканий), `ClientDetailPage` (tab-рефактор: hooks-order ок,
Overview збережено, manager-гейти=бек-403), `OrderDetailPage`/`TimeTab`, `CompanyGate` (жоден
маршрут не загублено).

## Відповідність дизайну (нові екрани = свідомі субсети)

Кожен новий екран — backend-backed субсет повного мокапа (RISK-нотатки у
[`DESIGN_COVERAGE.md`](DESIGN_COVERAGE.md)):

- **Leads** — ядро борду; **ще нема**: lead-detail `/leads/:id`, редактор пайплайнів,
  іконки джерел, UTM-захоплення з форми, суми угод по колонках, % конверсії, lost-аналітика.
- **Картка 360°** — 6/7 табів; нема «Секрети» (vault-модуль), агенційне edit-member обмежене
  (роль/видалити; нема reset-password/invite); People/Activity-rich-поля з мокапа.
- **Reports** — план-факт за оцінкою+capacity; нема 4-рівневого проєкт/місяць-розрізу дизайну.
- **Дошка задач** — cross-order DnD; нема per-team-boards/timeline-в'ю.
- **Project360** — білінг+юр-особа+маржа+close-cycle+нарахування; нема hero-KPI/hours-bar/задач.

## Стан гейту

type-check 23 · lint 14 · build 14 · test (**api 572 unit + 151 gated real-PG** · types 104) ·
3 міграції drift-free. CI `db-gate.yml` на пуші: migration-smoke + drift + RLS-isolation.

---

# 🔎 АУДИТ 2026-07-01 (раунд 2) — Vault-сесія (7 зрізів) + ремедіація

> Другий 4-вимірний багатоагентний аудит над діфом `71920b8..6cf86c4` (7 зрізів, 27 файлів,
> ~3k рядків): **security · typescript · code-quality · design+docs conformance**. Усі
> підтверджені знахідки виправлені; гейт зелений.

## Що збудовано за раунд

lead-detail `/leads/:id` · invite client member on-behalf · reset-password on-behalf ·
**модуль 17 Credentials Vault** (envelope AES-256-GCM, KEK у env; 360° таб + глобальний
`/vault` + per-secret журнал доступів + **2FA step-up повторним паролем**). api-тести 574 → **624**.

## Знахідки та виправлення (commit `fix(audit)` цього раунду)

**Вердикти агентів: 0 critical / 0 high на безпеці** (крипта, grant, RLS/owner-гейти — коректні,
перевірено, не лише за коментарями). Дизайн — чесний **задокументований субсет**, прихованих
прогалин нема.

| Sev            | Джерело | Знахідка                                                                                                | Виправлення                                                                                      |
| -------------- | ------- | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| **HIGH** (TS)  | ts      | reveal `setTimeout` без cleanup → `setState` на розмонтованому рядку + стек таймерів                    | спільний `SecretRow` з `useRef`+`useEffect`-cleanup; таймер чиститься перед новим                |
| **MED** (code) | quality | журнал доступів не перевіряв `credId ∈ companyId` → крос-клієнт info-leak у межах агенції               | existence-check `credentialVault.findFirst({id,companyId})` → 404 (як у reveal)                  |
| **MED** (sec)  | sec     | reveal-throttle по IP → lockout co-located owner'ів (availability)                                      | замінено на **per-owner** throttle (audit-count 10/год, spec §133) + `reveal_rate_limited` audit |
| **MED** (TS)   | ts      | невалідний `grant` (число/масив) у body → 500 замість 403                                               | `verifyRevealGrant` guard `typeof !== 'string'`; route звузив каст до `unknown`                  |
| **MED** (both) | ts+qual | `CredentialRow`≈`VaultRow` ~90-рядк. дубль + дрейф (глоб. екран без журналу; спільний mutation)         | витягнуто спільний `SecretRow` (per-row mutation; журнал тепер і на `/vault`)                    |
| **MED** (TS)   | ts      | `(err as {code})` каст — `onError` типовано `Error`                                                     | `STEP_UP_REQUIRED` привʼязано до `ApiErrorCode` enum; каст звужено до `{code?:unknown}`          |
| **LOW** (sec)  | sec     | `decryptSecret` не валідував довжини IV/tag (GCM усе одно кидає)                                        | додано length-guards (defense-in-depth)                                                          |
| docs           | design  | 4 застарілі місця: Leads-проза+зведення «zero code», 17-проза-лід «nothing real», 17-банер «greenfield» | датовані UPDATE-нотатки; зведення Leads `0→3 IMPL`; 17-банер оновлено                            |

**Не-баги (перевірено, доповідь лише бо аудит просив):** `setStepUpRetry(()=>retry)` коректний;
відсутність conditional-hooks; grant-singleton безпечний; SKEW правильний; `members.ts` reset —
cross-company IDOR закрито; envelope-крипта sound (унікальні DEK/IV, GCM-auth, DEK wipe у finally).

**Відкладено (задокументовано, не блокує):** `members.ts` → `withTenant` міграція (tenant-safe
сьогодні через explicit-agencyId); step_up_failed без companyId-контексту (у загальному audit-log є).

## Стан гейту (раунд 2)

type-check 23 · lint 14 · build 14 · test (**api 624 unit + 151 gated** · types 104) · 55/55 tasks.

---

# 🔎 РАУНД 3 (2026-07-02) — інфра/докс/архітектура: 3-агентний аудит + ремедіація

> Метод: 3 паралельні агенти (DevOps/deploy · архітектура/чистота коду · документація).
> Оцінки: **деплой 7/10 · архітектура 8/10 · доки 6.5/10**. Наскрізний патерн: «голова
> дисциплінована — хвіст гниє» (гейти є, але розірвано звʼязок перевірки з продом).

## Знахідки та виправлення (цей коміт)

| Sev            | Знахідка                                                                                                                                                            | Виправлення                                                                                                                                                                                                                                  |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **CRIT**       | RLS неможливо ввімкнути в проді: `DATABASE_APP_URL` ніхто не читав у коді, а `RLS_ENFORCED`/KEK не прокидались у контейнер — CI гейтив ізоляцію, якої прод не бачив | `packages/db`: клієнт бере `datasourceUrl` з `DATABASE_APP_URL` коли задано (web-only; migrate/worker — owner); compose staging+prod прокидає `RLS_ENFORCED`/`DATABASE_APP_URL`/`CREDENTIALS_KEK_BASE64` (суфіксовані імена в server `.env`) |
| **CRIT**       | `scripts/rollback.sh` без `--project-name` → у день інциденту піднімає ДРУГИЙ стек з конфліктними Traefik-лейблами замість відкотити робочий                        | `PROJECT_NAME=workflo-production` (= CI) + `--env-file` + `up --wait --wait-timeout 180`                                                                                                                                                     |
| **CRIT**       | Сід з фолбеком `Admin123!` біжить кожен staging-деплой, а dev-api/dev-portal відкриті в інтернет (whitelist був лише на workspace)                                  | seed fail-closed на `NODE_ENV=production` без `SEED_*_PASSWORD` (порожній = unset); compose прокидає `SEED_*`; api+portal під `TEAM_IPS`-whitelist з `/health` carve-out (CI-verify 200 збережено)                                           |
| **MED**        | `appleboy/ssh-action@v1`/`scp-action@v0.1.7` — рухомі теги з доступом до `HETZNER_SSH_KEY` (supply-chain ризик)                                                     | запінено по commit-SHA (v1.2.5 / v0.1.7) у staging+production (INFRA-SEC1 частково)                                                                                                                                                          |
| **MED**        | Авто-rollback `compose up -d` без `--wait` — міг рапортувати успіх на падаючих контейнерах                                                                          | `--wait --wait-timeout 180` (+ той самий фікс у rollback.sh)                                                                                                                                                                                 |
| **MED (docs)** | README-індекс не вів на ROADMAP і не знав аудитів після першого; TRACKER «СТАН ЗАРАЗ» відстав на 10 днів; лічильники брехали (8 cron ≠ 6 у коді, 7 ADR ≠ 8)         | README перезібрано (ROADMAP = start here, «Аудити»-lineage, чесний CRON-рядок, ПРАВИЛА = фактичний DoD-цикл + doc-drift-чек); TRACKER-знімок позначено історичним з вказівником; S4/S5-доки → `archive/`; AGENTS.md + Workflo-канон          |
| **LOW**        | `apps/landing/tsconfig.tsbuildinfo` закомічений (вічний шум у diff)                                                                                                 | знято з індексу + `*.tsbuildinfo` у `.gitignore`                                                                                                                                                                                             |

## Відкладено (беклог, не блокує)

- **Фронт-тести** (workspace 14k LOC / portal / landing — 0 юніт-тестів; компенсація — 1 smoke) — існуючий [test] FE-T1.
- **Розбиття `ClientDetailPage.tsx` (1288 рядків)** + хвіст (Settings 737 / OrderDetail 709).
- Staging-rollback + `.last_deploy`-after-verify (INFRA-OPS1) · offsite-restore drill (INFRA-DR1) · deploy-алерти (INFRA-OBS1) · reusable `check`-job (3 копії) · turbo remote cache · squash міграцій + конвенція імен · ERD/CRON_JOBS повний рефреш.

## Операційні дії власника (сервер, поза репо)

1. staging `.env`: `SEED_OWNER_PASSWORD` (+executor/client), `CREDENTIALS_KEK_BASE64_STAGING`, перевірити `TEAM_IPS`.
2. prod `.env`: `CREDENTIALS_KEK_BASE64_PROD`; для RLS-флipу (staging-соак першим): `ALTER ROLE workflo_app LOGIN PASSWORD…` → `DATABASE_APP_URL_*` + `RLS_ENFORCED_*=true`.
3. Наявним seed-акаунтам пароль сід не перезаписує — змінити вручну, якщо колись сіялись дефолтом.

## Стан гейту (раунд 3)

type-check/lint/test `@workflo/db`+`@workflo/api` зелені (**api 624 unit + 151 gated**); `docker compose config` валідний для staging+production; `bash -n rollback.sh` OK.
