// AR-42: canonical implementation lives in @workflo/app-core (shared portal + workspace).
export { useTelegramStatus, useTelegramConnect, useTelegramDisconnect } from '@workflo/app-core'
export type { TelegramStatus } from '@workflo/app-core'
