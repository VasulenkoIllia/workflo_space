import { InboxView } from '@workflo/app-core'

/** Portal inbox — the shared master-detail view (app-core), client-scoped subtitle. */
export function InboxPage() {
  return <InboxView subtitle="нових · по всіх ваших замовленнях + системні" />
}
