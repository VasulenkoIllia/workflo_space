import { InboxView } from '@workflo/app-core'

/** Workspace inbox — the shared master-detail view (app-core), team-scoped subtitle. */
export function InboxPage() {
  return <InboxView subtitle="нових · по всіх клієнтах і команді" />
}
